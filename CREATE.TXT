-- --------------------------------------------------------------------
-- --- DROP A TABLE ---------------------------------------------------
-- --------------------------------------------------------------------
-- DROP TABLE EMP;
-- COMMIT;

-- --------------------------------------------------------------------
-- --- CREATE A TABLE -------------------------------------------------
-- --------------------------------------------------------------------
   CREATE TABLE EMP
       (
           EMP_NO     SMALLINT       NOT NULL      PRIMARY KEY,
           EMP_NAME   VARCHAR(15)    NOT NULL,
           PROJECT    CHAR(10),
           JOIN_DATE  DATE           NOT NULL,
           SALARY     DECIMAL(8,2)   NOT NULL,
           DATE_TIME  TIMESTAMP      NOT NULL
       ) IN OZAGS3DB.OZAGS3TS;

-- --------------------------------------------------------------------
-- --- CREATE UNIQUE INDEX FOR PRIMARY KEY ----------------------------
-- --------------------------------------------------------------------
   CREATE UNIQUE INDEX IDX01 ON EMP(EMP_NO);

